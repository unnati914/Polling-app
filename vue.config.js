module.exports = {
    transpileDependencies: ["vuetify"],
    // Change the build paths to make then Maven compatible
    // see https://cli.vuejs.org/config/
    outputDir: "target/dist",
    assetsDir: "static"
  };